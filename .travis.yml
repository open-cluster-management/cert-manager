language: bash
sudo: true

language: go
go_import_path: github.com/jetstack/cert-manager
go:
  - 1.12

services:
  - docker

branches:
  only:
    - master
    - /^[0-9]+\..*$/
    - /^v[0-9]+\..*$/
    - /^release-[0-9]+\..*$/
    - build-harness

matrix:
  fast_finish: true

before_install:
  - source travis-env.sh

stages:
  - lint
  - test
  - build
  - publish

jobs:
  include:
    - stage: lint
      script:
        - make lint
    - stage: test
      os: linux
      script:
        - make go-fmt
        - make go-test
    - stage: build
      os: linux
      env:
        - PROJECT=acmesolver
      script:
        - make init
        - make build
    - stage: build
      os: linux-ppc64le
      env:
        - PROJECT=acmesolver
      script:
        - make init
        - make build
    - stage: build
      os: linux-s390
      env:
        - PROJECT=acmesolver
      script:
        - make init
        - make build
     - stage: build
      os: linux
      env:
        - PROJECT=cainjector
      script:
        - make init
        - make build
    - stage: build
      os: linux-ppc64le
      env:
        - PROJECT=cainjector
      script:
        - make init
        - make build
    - stage: build
      os: linux-s390
      env:
        - PROJECT=cainjector
      script:
        - make init
        - make build
    - stage: build
      os: linux
      env:
        - PROJECT=controller
      script:
        - make init
        - make build
    - stage: build
      os: linux-ppc64le
      env:
        - PROJECT=controller
      script:
        - make init
        - make build
    - stage: build
      os: linux-s390
      env:
        - PROJECT=controller
      script:
        - make init
        - make build
    - stage: build
      os: linux
      env:
        - PROJECT=webhook
      script:
        - make init
        - make build
    - stage: build
      os: linux-ppc64le
      env:
        - PROJECT=webhook
      script:
        - make init
        - make build
    - stage: build
      os: linux-s390
      env:
        - PROJECT=webhook
      script:
        - make init
        - make build
    - stage: publish
      os: linux
      script:
        - make docker:login
        - make push-images
    - stage: publish
      os: linux-ppc64le
      script:
        - make docker:login
        - make push-images
    - stage: publish
      os: linux-s390
      script:
        - make docker:login
        - make push-images
